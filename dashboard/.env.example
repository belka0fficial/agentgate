VITE_CLERK_PUBLISHABLE_KEY=
# Server-side only for Vite dev proxy; do not prefix with VITE_.
AGENTGATE_API_TARGET=http://127.0.0.1:8644
# Optional: if set in the server process, Vite proxy adds it server-side.
# Never expose this as VITE_*; browser must not receive it.
AGENTGATE_OWNER_TOKEN=
